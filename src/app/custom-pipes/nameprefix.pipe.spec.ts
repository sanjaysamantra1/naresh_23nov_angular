import { NameprefixPipe } from './nameprefix.pipe';

describe('NameprefixPipe', () => {
  it('create an instance', () => {
    const pipe = new NameprefixPipe();
    expect(pipe).toBeTruthy();
  });
});
